{
  // Cloudflare Workers — static assets deployment.
  // `npm run build` outputs to ./dist, which is served directly as static
  // assets (no Worker script needed). `npx wrangler deploy` uploads it.
  //
  // NOTE: "name" must match your Worker's name in the Cloudflare dashboard.
  // If your Worker is named something other than "farazian", change it here.
  "name": "farazian",
  "compatibility_date": "2026-06-01",
  // Worker runs first (to enforce the canonical host), then falls through to
  // the static assets via the ASSETS binding.
  "main": "worker.js",
  "assets": {
    "directory": "./dist",
    "binding": "ASSETS",
    "run_worker_first": true,
    // Serve public/404.html (copied to dist/404.html) for unknown routes,
    // with a real 404 status — instead of silently serving the homepage.
    "not_found_handling": "404-page"
  }
}
